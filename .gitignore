# The unencrypted site. Never commit this — GitHub Pages repos are public
# on free accounts, and this folder is the whole site in plain text.
source/

node_modules/
.DS_Store
# Original media uploads stay local; publish encrypted docs/media instead.
/assets/videos/
